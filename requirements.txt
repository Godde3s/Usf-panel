# Usf — requirements
# All dependencies are pure-Python or pre-built wheels, no compilation needed on HF Spaces.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
psutil>=5.9.0
# ─── Speed optimizations ────────────────────────────────────────────────────
uvloop>=0.19.0; sys_platform != "win32"
orjson>=3.9.0
httptools>=0.6.0
websockets>=12.0
